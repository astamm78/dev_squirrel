<h2>My Photography</h2>

<div class="post">

  <% if @photos %>
    <% @photos.each do |photo| %>
      <%= link_to (image_tag photo.image(:square)), photo.image(:original) %>
    <% end %>
  <% end %>

  <div class="spacer"></div>

</div>