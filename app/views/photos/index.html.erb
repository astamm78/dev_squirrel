<h2>My Photography</h2>

<% if @photos %>
  <% @photos.each do |photo| %>
    <%= link_to (image_tag photo.image(:square)), photo.image(:original), :class => 'photo_thumb' %>
  <% end %>
<% end %>

<div class="spacer"></div>